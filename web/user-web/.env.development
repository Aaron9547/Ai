# 路由：对话入口为 /{租户编码}/chat（与 sys_tenant.code 一致，非 /chat），见 PROJECT.md「★ 用户端路由与租户」。
VITE_TENANT_CODE=default
# 公网穿透域名强制未登录跳转登录页：默认关闭（与 IP 一致可访客聊天）。需要时设 VITE_AUTH_GATE_HOSTS=.onecraft.ca
# VITE_AUTH_GATE_HOSTS=false
# 说明：用「本机 IP:5173」与「localhost:5173」不是同一站点，localStorage 不共享；若 IP 站点里存了坏的用户 JWT，首屏拉模型会 401（http 已自动清会话并重试一次）。
# VITE_API_BASE：留空 = 请求发到当前页同源（经 Vite 代理到本机后端），用「本机 IP:5173」打开时通常不必配后端 CORS。
# 若填 http://localhost:8080 = 浏览器直连该主机上的 8080；用局域网 IP 打开页面时，代码在开发态会自动改回「走代理」，避免请求落到访问者本机 loopback。
# 若显式填 http://本机IP:8080 直连后端，与页面不同端口 → 跨域，须在管理端「跨域来源」加 http://本机IP:5173。
VITE_API_BASE=
VITE_TENANT_ID=1
