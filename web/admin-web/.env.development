# 本地联调且后端为 permit-all 等无需 Token 时，设为 true 可跳过登录路由守卫（勿用于生产）
# VITE_ADMIN_AUTH_SKIP=true
#
# VITE_API_BASE：留空 = 同源 + Vite 代理（用本机 IP:5174 打开一般不必配 CORS）；若填 http://…:8080 直连后端则与页面跨域，须在「跨域来源」加 http://本机IP:5174。
# VITE_API_BASE=
