# Ai 前后端 compose 配置（deploy/docker-compose.yml）
# 对接 super-agent deploy/infra 已有组件（mysql/redis/minio/milvus/elasticsearch/rocketmq）
# 与 super-agent deploy/infra/compose.env 密码保持一致即可

# ---------------------------------------------------------------------------
# 接入已有 Docker 网络（super-agent-infra）
# ---------------------------------------------------------------------------
COMPOSE_NETWORK_NAME=super-agent-infra

# ---------------------------------------------------------------------------
# 本地镜像名
# ---------------------------------------------------------------------------
IMAGE_PREFIX=ai-platform
IMAGE_TAG=0.1.258-SNAPSHOT

# ---------------------------------------------------------------------------
# 对外端口
# ---------------------------------------------------------------------------
USER_WEB_PORT=5173
ADMIN_WEB_PORT=5174

# ---------------------------------------------------------------------------
# MySQL（同 infra 内 mysql 服务；库名 ai 须已执行 db/mysql/schema_v1.sql）
# ---------------------------------------------------------------------------
MYSQL_URL=jdbc:mysql://mysql:3306/ai?useUnicode=true&characterEncoding=utf8&connectionTimeZone=%2B08%3A00&forceConnectionTimeZoneToSession=true
MYSQL_USERNAME=root
MYSQL_PASSWORD=Root@123456

# ---------------------------------------------------------------------------
# Redis（与 infra compose.env 一致）
# ---------------------------------------------------------------------------
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_PASSWORD=Redis@123456
SPRING_DATA_REDIS_DATABASE=0

# ---------------------------------------------------------------------------
# MinIO
# ---------------------------------------------------------------------------
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# ---------------------------------------------------------------------------
# Milvus
# ---------------------------------------------------------------------------
AI_MILVUS_HOST=milvus
AI_MILVUS_PORT=19530

# ---------------------------------------------------------------------------
# Elasticsearch
# ---------------------------------------------------------------------------
AI_RAG_ES_ENABLED=true
AI_RAG_ES_HOST_PORTS=elasticsearch:9200
AI_RAG_ES_CLUSTER_NAME=es-ai

# ---------------------------------------------------------------------------
# RocketMQ
# ---------------------------------------------------------------------------
AI_ROCKETMQ_ENABLED=true
ROCKETMQ_NAME_SRV=rocketmq-namesrv:9876
ROCKETMQ_PRODUCER_GROUP=ai-producer

# ---------------------------------------------------------------------------
# 鉴权
# ---------------------------------------------------------------------------
AI_DISCOVERY_ENABLED=false
AI_AUTH_JWT_LOCAL_SECRET=super-agent-jwt-secret-change-in-production-32b
AI_LLM_MODEL_KEY_SECRET=
AI_DEFAULT_TENANT_ID=1
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
