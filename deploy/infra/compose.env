# Ai 基础设施编排（deploy/infra/docker-compose.yml）
# 生产：复制为 deploy/infra/.env 覆盖

# ---------------------------------------------------------------------------
# Docker Hub 加速（daemon.json 仍建议配置 registry-mirrors）
# ---------------------------------------------------------------------------
DOCKER_HUB_MIRROR=docker.m.daocloud.io

# ---------------------------------------------------------------------------
# 对外端口
# ---------------------------------------------------------------------------
MYSQL_PORT=3306
REDIS_PORT=6379
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MILVUS_PORT=19530
ES_HTTP_PORT=9200
ROCKETMQ_NAMESRV_PORT=9876

# 持久化目录（默认与 super-agent 风格一致；可改为宿主机绝对路径）
# MYSQL_DATA_DIR=/vol1/1000/docker-data/mysql
MYSQL_ROOT_PASSWORD=Root@123456
MYSQL_DATABASE=ai

# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------
SPRING_DATA_REDIS_PASSWORD=Redis@123456

# ---------------------------------------------------------------------------
# MinIO
# ---------------------------------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ---------------------------------------------------------------------------
# Elasticsearch
# ---------------------------------------------------------------------------
ES_JAVA_OPTS=-Xms512m -Xmx512m
